ktor {
    deployment {
        port = 8008
        port = ${?PORT}
    }
    application {
        modules = [ com.lifetrack.ApplicationKt.module ]
    }
}
jwt {
    domain = "http://0.0.0.0:8008"
    audience = "jwt-users"
    realm = "lt backend application"
    secret = "baba yao"
}
